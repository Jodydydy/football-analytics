# Заполняется по мере переноса — держим минимальным и с верхними границами.
# Правило: пакет попадает сюда только когда перенесён код, который его использует.

ultralytics>=8.3.0
supervision>=0.25.0
opencv-python>=4.10.0
numpy>=1.26.0
torch>=2.2.0
torchvision>=0.17.0
onnxruntime>=1.17.0
matplotlib>=3.8.0
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
